name: Fork Smoke Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: python3 -c "import pathlib; assert pathlib.Path('mlx').is_dir(); print('ok')"